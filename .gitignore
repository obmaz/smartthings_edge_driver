# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
.vscode
# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
/.idea/
*.iml

# macOS metadata
.DS_Store

# Local Lua language server config with machine-specific SDK paths
.luarc.json
